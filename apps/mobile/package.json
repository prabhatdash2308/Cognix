{
  "name": "@cognix/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Cognix mobile application — placeholder.",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
