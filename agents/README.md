# Agents

Autonomous agent runtimes and workflows for Cognix live here. Each agent
package is picked up automatically by the `agents/*` workspace glob and builds
on `@cognix/ai` and `@cognix/types`.

No agents exist yet.
