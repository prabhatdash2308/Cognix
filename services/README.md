# Services

Backend microservices for Cognix live here. Each service is an independent,
deployable unit (its own `package.json` or `pyproject.toml`) and is picked up
automatically by the `services/*` workspace glob.

No services exist yet.
